Adding CSS to an HTML page.


styling:

 Body width o 600px, centered by setting the margin to 0 auto
 Font for the body = font-family of Lucida sans and a colour of #00376c

 Anchors:
 colour = #00376c
level 3 headings colour == #1577ff

.services paragraphs + footer font == italic
all anchors text-decorations == none + hover state, set text-decoration underline

h2 solid border-bottom == 2px
h2 solid border-colour == #7b7b7b
h2 text-align == center

h3 border-bottom == 2px
h3 colour == #1577ff
h3 display prop == inline


layout
h1 == solid 6px border-bottom
h1 == colour #7